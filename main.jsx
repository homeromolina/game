import React from 'react'
import ReactDOM from 'react-dom/client'
import GunboundAllStars from './gunbound-allstars.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <GunboundAllStars />
    </React.StrictMode>,
)
